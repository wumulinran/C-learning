mysql的数据类型：

数值 时间  字符串

tinyint   1
smallint  2
mediumint 3
int       4
bigint     8
float      4
double      8
decimal     a+2     
decimal(a,b) a指定指定小数点左边和右边可以存储的十进制数字的最大个数，最大精度38。 b指定小数点右边可以存储的十进制数字的最大个数。小数位数必须是从
0 到 a之间的值。默认小数位数是 0。 一般用于存储金钱     整数位最大为（a-b） 小数位最大为b  但是存储时可接受大于b的小数位 （四舍五入）
（参考：https://blog.csdn.net/u010002184/article/details/79238624）

DATE YYYY-MM-DD  3
TIME HH:MM:SS   3
YEAR YYYY        1
DATETIME YYYY-MM-DD HH:MM:SS  8
TIMESTAMP YYYYMMDDHHMMSS       4

CHAR        定长字符串
VARCHAR     可变长字符串
TINYBLOB    二进制形式的短文本数据
BLOB        二进制形式的文本数据
MEDIUMBLOB  二进制形式的中等文本数据
LONGBLOB    二进制形式的超大的文本数据
TINYTEXT    短文本字符串
TEXT        文本数据
MEDIUMTEXT  中等长度文本数据
LONGTEXT    超大的文本数据



mysql 基本语法
1.show databases; //查看数据库
2.use (databaseName);//选择数据库
3.drop (databaseName);//删除数据库
4.show tables;//查看所有表
5.desc (tableName);//查看表的描述信息
6.create table (tableName) (字段1 类型1,字段2 类型2); //创建表
7.drop table (tableName);//删除一张表
8.rename table (oldtableName) to (newtableName);//重命名表

表内字段修改 通过alter
1.alter table (tableName) add column (字段名) (字段类型); //表添加一个字段
2.alter table (tableName) update column (字段名) (字段类型); //修改表内的字段
3.alter table (tableName) drop column (字段名); //删除表内的字段
4.alter table (tableName) primary key(字段名); //将字段设置为主键
5.alter table (tableName) add constraint (外键名) foreign key(外键字段名) REFERENCES 外表表名(对应的表的主键字段名);

mysql 基本的增删改查
增 insert into (tableName)[可选字段] values(参数1)[,(参数2)]; 
删 delete from (tableName) where (定位条件);
改 update (tableName) set (字段1)=(value1),(字段2)=(value2)... where (定位条件);
查 select (字段1),(字段2) from (tableName) where (定位条件);

字段修饰 https://blog.csdn.net/weixin_42867972/article/details/82823011
null   not null    default     unsigned    auto_increment    primary key    unique key    zerofill
eg: create table course(course_id varchar(20) primary key,deptnames varchar(20) not null,credits int, foreign key(deptnames) references department(dept_name));

外键的相关设置：
级联操作: on delete[update] cascade 
置为null: on delete[update] set null

数据库三范式
作用：约束数据库建表的规范性
最终目标：不存在冗余数据

1NF.列的原子性  该列不可再拆分
2NF.存在多对多关系时只有一个字段作为主键是不够的， 部分依赖，会产生冗余数据，需要分解表   (eg:老师与学生的关系 多对多)
3NF.表中不能出现依赖传递，非主键的列只能依赖于主键



查询语法详解：
where  条件比较
单值判断： <, >, <>, !=, =, >=, <=, is null, in not null
区间判断：betwteen x and y -> [x,y]
数据集包含判断: in   not in
数据集单值判断 结合单值判断： any some all  (some 是any的别名)
模糊查询 like  _ %      _->占位符 一个占一位   %代表可以不占，也可以占无数位

逻辑复合条件  and  or

order by 排序  
升序asc   desc  可多个字段同时排序 优先级从高到低

创建计算字段
sql中允许select子句中出现+,-,*,/ 
 

    
